# Mozy environment variables (template)
#
# Copy this file to `.env` and fill in if you want to override the defaults.
# `.env` is gitignored — never commit real secrets.
#
# NOTE: Currently the project reads keys directly from source files, not env.
# This file documents the values for reference and as a placeholder for a
# future env-based refactor. You do NOT need to create a .env to run the app.

# ---- Supabase (publishable / anon key, safe to expose) ----
# Hardcoded in src/data/supabase.ts
SUPABASE_URL=https://ewmfbkrlfjytjzaxbvex.supabase.co
SUPABASE_ANON_KEY=sb_publishable_34gHz5ZDBGrfwocQ7oOa-Q_HfgLSE-H

# ---- 서울 열린데이터광장 - 지하철 실시간 도착 API ----
# Hardcoded in src/data/seoulMetroApi.ts
# Get your own at https://data.seoul.go.kr if you want to swap
SEOUL_METRO_API_KEY=4e457a6a4b73756e3130396a42744c
